/**
 * world.timeline_player core
 *
 * This module exports only non-UI core logic (engine + constants).
 * UI (HUD/panels) must live in module UI files, not in core.
 */
export { TimelinePlaybackEngine, ANIMATION_COLOR_SCHEMES } from "../../core/timelinePlayer.js";

